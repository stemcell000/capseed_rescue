<%= search_form_for @q, :url => display_all_assays_path, :class => "form-inline" ,:remote => true do |f| %>
  
  <%= f.label "Search by any terms or value:" %>
  <%= f.search_field :name_or_clones_name_cont %>
  
  <%= f.submit %>

<% end %>