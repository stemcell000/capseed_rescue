<%= smart_listing_item :clones, :update, @clone, @clone.valid? ? "clones/smart_listing/clone" : "clone_batches/components/edit_from_inventory" %>

//Popover
 $('[data-toggle="popover"]').popover({html:true});