	  <table  class="table table-striped table-small">
	    <thead>
		    <th class="col-lg-2 left">
		        <%= smart_listing.sortable "Primer", :name %>
		    </th>
		    <th class="col-lg-4 left">
		        <%= smart_listing.sortable "Sequence", :sequence %>
		    </th>
		    <th class="col-lg-2 center">
		        <%= smart_listing.sortable "Available", :available %>
		    </th>
		    <th class="col-lg-3 center">
		        <%= smart_listing.sortable "comment", :comment %>
		    </th>
		  	<th class="col-lg-1 center"> Actions</th>
	    </thead>
	    <tbody>
		  	<% smart_listing.collection.each do |p| %>
	      		<tr class="editable" data-id = "<%= p.id %>">
					<%= smart_listing.render object: p, partial: "primers/primer", locals: {object: p} %>
	        		</tr>
	      	<% end %>
	     <%= smart_listing.item_new colspan: 9, link: new_primer_path(:object) %>
	    </tbody>
	  </table>
	  <%= smart_listing.paginate %>
	  <%= smart_listing.pagination_per_page_links %>
