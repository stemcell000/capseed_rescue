%h1 Listing clones

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @clones.each do |clone|
      %tr
        %td= link_to 'Show', clone
        %td= link_to 'Edit', edit_clone_path(clone)
        %td= link_to 'Destroy', clone, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Clone', new_clone_path
