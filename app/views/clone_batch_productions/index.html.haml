%h1 Listing clone_batch_productions

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @clone_batch_productions.each do |clone_batch_production|
      %tr
        %td= link_to 'Show', clone_batch_production
        %td= link_to 'Edit', edit_clone_batch_production_path(clone_batch_production)
        %td= link_to 'Destroy', clone_batch_production, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Clone batch production', new_clone_batch_production_path
