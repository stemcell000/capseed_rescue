	//Les donnees dans les champs du formulaire form_clone sont testé d'apres les criteres de validation
	//definis dans le model. Si ces criteres sont verifies, les donnees sont sauvees. Si non, le formulaire
	//dans sa fenetre modale reste ouvert et les message d'erreur s'affichent en haut du formulaire NEW.
		$("#myModalLabel").html("Clone batch")
		$("#myModalButtons").html("<%= escape_javascript( render'components/modal_close_btn' ) %>")
		$("#flash-msg-container").html("")
		$("#inner-assay-modal").html("<%= escape_javascript( render :partial => 'clone_batches/components/new_as_plasmid', :locals => {:clone => clone, :assay => @assay}) %>")
		$("#assay-modal").modal("show")
		$("ul.errors").html("")
