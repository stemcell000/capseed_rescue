		<%= simple_nested_form_for @option, url: display_all_clone_batch_switch_option_path(@option),  html: { class: 'form-inline' }, :remote=> true do |f| %>
		  	 Display hidden plasmids <%= f.input :display_all_clone_batch, :as => :boolean, :label =>false, :input_html => {:onclick => "$('#option_display_all_clone_batch*').submit();"}%>
		<%end%>

