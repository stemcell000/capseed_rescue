		$("#qc-label").html("Clone batch Quality Control: PCR Colony")
		$("#qc-buttons").html("<%= escape_javascript( render'components/modal_close_btn' ) %>")
		$("#flash-msg-container").html("")
		$("#inner-qc-modal").html("<%= escape_javascript( render :partial => 'pcr_colonies/pcr_colony_components/form', locals: {:clone_batch_id => @clone_batch.id, :clone_id => @clone.id, :assay_id => @assay.id} ) %>")
		$("ul.errors").html("");
		
		$("#qc-modal").modal("show");
		
	console.log("new QC");
