  <td><%=link_to object.name, fetch_virus_batches_box_path(object.id), :remote => true %></td>
  <td><%= object.barcode %></td>
<td class="actions text-right">
	<% if can? :manage, Box%>
        <%= link_to "<i class='fas fa-pen-square'></i>".html_safe, edit_box_path(object), method: :get %>
  	<%end%>
</td>